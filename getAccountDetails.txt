PARAMETERS varLocCode
IF !USED("CIBIMP_MST")
	DO usetables WITH "CIBIMP_MST"
ENDIF
SELECT CIBIMP_MST
SET FILTER TO
GO top
LOCATE FOR ALLTRIM(UPPER(CIBIMP_MST.ACCTNO)) = ALLTRIM(UPPER(varLocCode))
IF FOUND()
	RETURN alltrim(upper(CIBIMP_MST.ACCTNAME))
	ELSE
	RETURN ""
endif